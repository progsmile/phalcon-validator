language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - 7.4

services:
  - mysql

sudo: false

before_install:
  - composer self-update
  - travis_retry composer install --prefer-source

script:
  - vendor/bin/phpunit --coverage-text
